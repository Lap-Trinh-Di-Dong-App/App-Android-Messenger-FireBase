<?xml version="1.0" encoding="utf-8"?>

<!--Part 5-->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile_image_userItem"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="UserName"
        android:id="@+id/userName"
        android:layout_toRightOf="@+id/profile_image_userItem"
        android:layout_toEndOf="@+id/profile_image_userItem"
        android:layout_marginLeft="10dp"
        android:layout_centerVertical="true"
        android:textSize="18sp"/>

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="15dp"
        android:layout_height="15dp"
        app:civ_border_width="10dp"
        app:civ_border_color="@color/colorPrimaryDark"
        android:id="@+id/image_on"
        android:visibility="gone"
        android:layout_below="@+id/userName"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="15dp"
        android:layout_toRightOf="@+id/profile_image_userItem"
        android:layout_toEndOf="@+id/profile_image_userItem"
        android:src="@mipmap/ic_launcher"/>

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="15dp"
        android:layout_height="15dp"
        app:civ_border_width="10dp"
        app:civ_border_color="#bfbfbf"
        android:id="@+id/image_off"
        android:visibility="gone"
        android:layout_below="@+id/userName"
        android:layout_marginTop="-5dp"
        android:layout_marginLeft="250dp"
        android:layout_toLeftOf="@+id/profile_image_userItem"
        android:layout_toEndOf="@+id/profile_image_userItem"
        android:src="@mipmap/ic_launcher"/>

</RelativeLayout>